apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: rabbitmq-cluster-operator-config

commonLabels:
  app: rabbitmq-cluster-operator
  managedBy: argocd
  managementCodeGroup: chrismlee42-dev
  managementCodeProject: rabbitmq-cluster-operator-config

resources:
  # https://github.com/rabbitmq/cluster-operator/releases
  - https://github.com/rabbitmq/cluster-operator/releases/download/v2.20.1/cluster-operator.yml
